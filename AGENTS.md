# 工作规则
若存在本机 `TASK_STATE.md`，先读取它；再读 `docs/INDEX.md` 和 `git status`，仅加载相关模块。项目约束见 `docs/ENGINE.md`、`docs/DATA_SOURCES.md` 与 `docs/HOSTING.md`；操作和恢复见 `docs/OPERATIONS.md`。

- 项目包括单用户 Windows 本机应用与无服务器浏览器版。本机 API 只监听回环地址；代码、运行数据、备份分别存放。
- 仅做多。三个市场和币种独立。指数只用于信号；真实数据和合成演示隔离。
- 策略只能看到决策时刻可用的数据；订单数量在决策时确定，下一交易日成交。
- 严格 VWAP 缺口阻止回测；不能静默使用收盘价替代。未复权交易与公司行动分别记账。
- 参考历史价格必须使用独立研究模式、清晰提示与结果标记；不重复计拆股或虚构股息，不冒充时点有效的实际交易回测。
- 行情分区和回测快照不可变，更新不得破坏旧结果。密钥不进代码、日志和普通备份。
- 金额 Decimal 计算、现金流与收益分开；账本/时序/迁移变化须做针对性回归。
- 代码更新默认只做逻辑/语法检查和最相关的少量测试，不自动运行全套测试。按影响范围选择验证，只有新失败或未解决风险需要时才扩大范围。
- 保留现有改动。逻辑变更更新本机 `docs/changes/` 与 `TASK_STATE.md`；每次项目更新使用 `scripts/publish_update.ps1` 刷新桌面快捷方式、暂存明确路径、提交并尝试普通推送。不得 force push。
- 本机 `TASK_STATE.md`、`docs/changes/`、个人需求文档、个人 `config/universe.yaml`、运行验证 JSON、个人策略/研究、行情、日志、凭据与备份不属于公开源码，不得纳入发布。发布脚本会阻止这些本机状态/配置文件，并在 Git 历史含这些内容时拒绝推送；公开分支必须以 `.public-history-baseline` 标记的隐私筛选根提交开始，原历史只保留在本机。
- GitHub Pages 只托管静态文件，不运行 FastAPI/SQLite。当前本机应用没有登录或租户隔离，也会执行可信模型 Python 策略，禁止直接暴露至公网。浏览器版仅运行内置策略，数据留在访客浏览器。
- 迁移和恢复先做一致性备份，恢复到新目录。备份清理默认只预览，禁止删除唯一副本。
- 阶段结束或中断前保存当前事实、缺口、验证和下一步。不得把离线测试当作真实数据验收。
