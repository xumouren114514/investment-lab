# 变更日志

## 策略参数表单与内置策略扩展（2026-09-24）

- 移除本机版和 GitHub Pages 版“更简单的常用设置／策略说明与定投”卡片，改为从共享目录动态展示策略说明和参数表单；旧策略 ID、配置参数 JSON、命名配置及高级编辑继续兼容。
- 保留原有 9 种策略，新增自适应定投、目标比例配置、EMA、MACD、时序动量、唐奇安突破、分层回撤、RSI、布林带、随机指标、ATR 风险仓位、波动率目标和反波动率配置，共 22 种。
- 每月自动入金单独放在资金计划区域，继续只增加现金；定投买入金额由所选策略参数控制。
- 策略运行只使用决策时可用历史数据，并按下一交易日模拟执行；指标预热或必要行情不足时跳过信号，不切换价格字段。
- 本机回测冻结策略目录与程序；Pages 构建校验并记录引擎资源清单，避免页面与浏览器计算模块版本不匹配。

## 月度动态等权策略（2026-09-24）

- 浏览器版增加月度动态等权预设，支持任意数量的已选标的、月度现金流、低配优先补仓、5 个百分点偏离再平衡、不卖出模式及指定检查日期。
- 该策略作为固定内置代码在浏览器 Worker 中运行；仍不执行访客上传的任意 Python 文件。

## 首次公开部署与发布流程维护（2026-09-24）

- 发布公开浏览器版：<https://xumouren114514.github.io/investment-lab/>；页面由 GitHub Pages 提供，回测和用户数据仍留在访客自己的浏览器。
- 启用 GitHub Actions 作为 Pages 来源；`main` 推送会自动构建并部署。
- 将 Actions 固定版本更新到 Node.js 24 运行时并锁定完整提交 SHA，运行器固定为 Ubuntu 24.04，避免弃用告警和即将迁移的 `ubuntu-latest` 标签。
- 用五日合成样本验证了单次买入持有、每日滚动及开发区间/留出三种线上浏览器 Worker 流程、进度展示和结果保存；该验证不代表真实行情或投资效果，也未覆盖真实 `.json.gz` 文件导入。

## 浏览器版预览（2026-09）

- 新增静态浏览器应用与 GitHub Pages 工作流；构建器只复制明确列出的公开文件，并拒绝构建目录中的额外文件和链接。
- 在浏览器 Worker 中运行固定版本 Pyodide 和内置策略；导入快照包、在 IndexedDB 本地保存数据与研究、导出备份。
- 支持单次、滚动起点及留出研究的配置界面、费用/现金流参数、进度与研究结果展示。滚动和留出网页流程仍需专项验收。
- GitHub Actions 固定到完整提交 SHA，并按最小权限运行；Dependabot 定期检查动作版本。
- 为公开仓库提供空白通用标的池模板，并在初始化时保留现有本机配置；发布基线排除个人标的池和完整需求文档。

## 本机研发版

本机版包含回测账本、数据来源适配、配置保存、快照导入/导出、备份恢复、桌面启动入口及 Python 策略 API。该版本仍按单用户设计，只监听回环地址；严格 VWAP、数据来源和未核验的历史交易规则限制保持生效。
